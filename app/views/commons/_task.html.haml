= link_to board_task_path(task) do
  .board 
    .board_title
      = task.title
    .board_content
      = task.content
    .board_detail
      = image_tag 'Oval.png'
      %div
        %p= task.author_name